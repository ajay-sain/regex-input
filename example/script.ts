import { RegexInput, Label } from "../src/index";

document.head.appendChild(document.createElement("title")).innerText = "Regex Input Example";
document.body.appendChild(RegexInput());
document.body.appendChild(document.createElement("hr"));
document.body.appendChild(Label("regex-input"));